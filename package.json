{
  "name": "penrecon",
  "version": "1.0.0",
  "description": "Single-user, local pentest recon manager. Ingest scan output (nmap XML today), browse hosts/services, diff two scans, and keep your own notes/tags/attachments on findings. FastAPI + HTMX, SQLite, no auth — binds to `127.0.0.1`.",
  "main": "index.js",
  "directories": {
    "test": "tests"
  },
  "scripts": {},
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "@types/node": "^26.1.0"
  }
}
